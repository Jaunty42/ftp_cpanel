name: FTP Test

on:
  push:
    branches: [master]

jobs:
  deploy:
    name: 🚀 Deploy website every commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 git Sync files
        uses: ./
        with:
          server: ftp.firenos.com
          username: ai@hexcody.com
          password: Prodigy#4242
